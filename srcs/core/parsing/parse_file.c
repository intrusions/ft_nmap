#include "inc.h"

bool parse_file_from_arg(char *file, t_options *opts)
{
    (void)file, (void)opts;
   
   // open file

   // gnl on each line
   
   // store the ** in opts struct

    //verify if its valid ip/hostname

    return true;
}
